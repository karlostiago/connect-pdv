plugins {
	id 'java'
	id 'org.springframework.boot' version '3.2.4'
	id 'io.spring.dependency-management' version '1.1.4'
}

allprojects {
	group = 'com.connect-pdv'
	version = 'SNAPSHOT'
	sourceCompatibility = '17'
}

subprojects {
	apply plugin: 'java'
	apply plugin: 'org.springframework.boot'
	apply plugin: 'io.spring.dependency-management'

	sourceCompatibility = '17'

	repositories {
		mavenCentral()
	}
}

repositories {
	mavenCentral()
}

dependencies {
	compileOnly project(':commons')
	compileOnly project(':cadastro')
	compileOnly project(':pdv')
	compileOnly project(':web')
	compileOnly project(':fiscal')
	compileOnly project(':faturamento')
}

tasks.named('test') {
	useJUnitPlatform()
}

tasks.register('bootRunCadastro') {
	dependsOn ':cadastro:bootRun'
}

tasks.register('bootRunFaturamento') {
	dependsOn ':faturamento:bootRun'
}

tasks.register('bootRunPdv') {
	dependsOn ':pdv:bootRun'
}

tasks.register('bootRunFiscal') {
	dependsOn ':fiscal:bootRun'
}

project(':faturamento') {
	dependencies {
		compileOnly project(':commons')
	}
}

project(':pdv') {
	dependencies {
		compileOnly project(':commons')
	}
}

project(':cadastro') {
	dependencies {
		compileOnly project(':commons')
	}
}

project(':fiscal') {
	dependencies {
		compileOnly project(':commons')
	}
}
